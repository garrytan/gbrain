- 所有模板以 `*.template` 后缀命名，并在首行添加 `Generated by soul-audit` 注释，表明其为可再生成的脚手架而非最终文档。
- 需要交互式或外部信息填充的位置统一使用 HTML 注释占位符（如 `<!-- add work contacts -->`、`<!-- auto-populated from git config -->`），便于审计阶段识别并替换。