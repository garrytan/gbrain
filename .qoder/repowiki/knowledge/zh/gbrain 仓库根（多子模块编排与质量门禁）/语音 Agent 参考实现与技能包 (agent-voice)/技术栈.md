Node.js ESM (`type: module`) + 原生 `http` 服务器；OpenAI Realtime `/v1/realtime/calls` 作为 LLM 后端；WebRTC data channel 承载工具调用；Twilio Media Stream 为可选电话入站路径；测试使用 Vitest + Puppeteer，E2E 音频固定样本位于 `tests/e2e/audio-fixtures/*.wav`。