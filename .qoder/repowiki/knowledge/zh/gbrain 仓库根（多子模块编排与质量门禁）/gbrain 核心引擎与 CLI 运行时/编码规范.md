- 子命令文件位于 commands/ 下，文件名即 CLI 子命令名，统一通过 EngineFactory 获取 Engine 实例后调用其方法完成工作
- AI 模型调用必须经 ai/gateway.ts 路由，禁止在子域中直接构造 provider 客户端
- 数据库访问统一通过 db.ts / connection-manager.ts 提供的连接池，禁止绕过连接管理直接 new Client
- 所有数据变更通过 commands/migrations/*.ts 中的 in-process 迁移脚本声明式演进，版本号与 schema.sql 保持同步