TypeScript/ESM；向量检索依赖外部 embedding provider（OpenAI text-embedding-3-large / Voyage v3+ / ZeroEntropy zembed-1 等，通过 `core/ai/gateway.ts` 抽象）；RRF 常数 `k=60`、余弦重评分 0.7×RRF+0.3×cosine；跨模态路径使用 D1 上的 HNSW 列（`embedding-column.ts`）。