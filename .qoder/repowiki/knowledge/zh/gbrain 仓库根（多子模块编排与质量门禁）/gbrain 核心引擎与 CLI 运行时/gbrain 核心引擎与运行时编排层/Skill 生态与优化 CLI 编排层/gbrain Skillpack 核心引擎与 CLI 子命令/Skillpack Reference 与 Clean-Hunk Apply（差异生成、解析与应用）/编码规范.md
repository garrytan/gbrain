- 统一使用 TypeScript interface 定义输入选项与返回结果（如 ReferenceOptions、ReferenceResult、ParsedDiff、ApplyHunksResult），并通过类型字面量联合限定 status 字段（'identical' | 'differs' | 'missing' 等）。
- 对每段逻辑添加 JSDoc 注释说明其契约与局限（特别是两路合并无法区分用户改动与 bundle 改动的约束），并在代码内以 TODO/D-number 标注后续计划。
- 文本分割与拼接采用统一的 split/join 模式：先判断是否以换行结尾，剥离后 split('\n')，修改后再 join('\n') 并按需补回尾部换行，避免 off-by-one。
- 二进制文件检测通过 Buffer.includes(0) 快速判定，遇到二进制直接跳过文本 diff/apply 并返回专用状态，不在上层分支中混入二进制逻辑。