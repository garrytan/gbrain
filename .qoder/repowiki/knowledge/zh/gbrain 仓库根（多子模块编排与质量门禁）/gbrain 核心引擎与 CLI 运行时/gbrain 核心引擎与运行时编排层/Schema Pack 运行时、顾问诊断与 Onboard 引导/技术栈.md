Zod 用于 Schema Pack Manifest v1 的结构校验与向前兼容（如 `extractable: boolean | ExtractableSpec`  widening、`verifier_path` 预留字段拒绝）；PostgreSQL 原生 SQL 通过 `engine.executeRaw` 直接下发，并在大表场景使用 `TABLESAMPLE BERNOULLI` 采样（entity link coverage ≥50K 时）。