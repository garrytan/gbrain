- 架构文档只描述当前行为与负载不变式，禁止写入 per-release 叙事，变更通过 CHANGELOG.md 与 git log/blame 维护并由 CI 校验。
- 跨模块引用统一通过 `docs/architecture/KEY_FILES.md` 的“每文件条目”路由，编辑前先读对应条目而非直接翻源码。
- MCP 客户端指南采用“README 总览 + 每个客户端一个 md 文件”的并列结构，安装命令保持可复制粘贴的一行格式。
- 部署片段放在 `guides/<topic>-deployment-snippets/` 子目录下，文件名带 `.partial` 或 `.example` 后缀以示不可直接运行。