单仓命令即入口：`bun dev` 启动 CLI，`bun build` 编译二进制，`bun build:admin` 先构建 admin_frontend 再内嵌；`bun test` 并行跑 unit，`bun test:full` 串联 verify+unit+slow+E2E；`bun check:all` 一次性执行 scripts/ 下全部门禁脚本；`bun ci:local` 通过 docker-compose 拉起依赖并运行 diff 感知 E2E。