- 每个测试文件顶部用注释标注关联的需求编号（如 v0.38 T2、v0.41 T3、T8 D13），将用例与需求追踪一一对应。
- 通过本地工厂函数（`minimalManifest`、`makeManifest`、`chainLoader`）构造最小化 `SchemaPackManifest`，只声明被测 API 所需的字段，其余省略。
- 对可能抛出错误的场景统一使用 `expect(() => fn(...)).toThrow(ErrorClass)` 或 try/catch + `toBeInstanceOf` 双重断言，同时校验错误 message 片段与 code 字段。
- 涉及全局状态（pack 缓存、审计开关）的用例在 `beforeEach` 中调用 `_resetPackCacheForTests()`，并用 `withEnv({...}, async () => ...)` 包裹异步分支以隔离环境变量。
- 隐私约束通过正则拼接（字符串拼接避免被 grep 守卫误伤）断言输出不包含真实人名/公司名，并在注释中引用 CLAUDE.md 隐私规则。