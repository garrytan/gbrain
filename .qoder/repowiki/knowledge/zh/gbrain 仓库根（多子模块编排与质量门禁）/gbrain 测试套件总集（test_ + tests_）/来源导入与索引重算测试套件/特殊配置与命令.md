所有涉及 DB 的测试在 `beforeAll` 中 `new PGLiteEngine()` → `connect({})` → `initSchema()`，并在 `afterAll` 中 `disconnect()`；需要隔离文件系统或 git 行为的测试（如 `sources-ops.test.ts`）通过 `withEnv` 注入 `GBRAIN_HOME` 和伪造 `git` 可执行文件，避免真实网络与磁盘副作用。