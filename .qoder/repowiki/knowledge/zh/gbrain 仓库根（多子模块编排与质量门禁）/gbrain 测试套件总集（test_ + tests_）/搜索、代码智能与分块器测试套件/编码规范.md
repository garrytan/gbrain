- 每个测试文件顶部用注释标注版本/代号（如 `v0.19.0 Layer 5`、`T3`、`W3`），将变更与发布波次关联。
- 涉及 PGLite 的用例统一采用 `beforeAll` 创建并初始化引擎、`afterAll` 断开连接、`beforeEach` 调用 `resetPgliteState(engine)` 清理状态的三段式模板。
- 需要数据库种子的用例通过 `engine.executeRaw` 直接 INSERT `pages`/`content_chunks`/`code_edges_symbol` 等表的最小行，而非调用高层导入 API。
- 断言优先使用 `expect(...).toBeCloseTo(expected, 6)` 处理浮点分数，避免精度抖动导致的 flaky。
- 故意跨文件共享状态或无法隔离的用例通过追加 `.serial.test.ts` 后缀显式声明串行执行，而非留在 allowlist 中。