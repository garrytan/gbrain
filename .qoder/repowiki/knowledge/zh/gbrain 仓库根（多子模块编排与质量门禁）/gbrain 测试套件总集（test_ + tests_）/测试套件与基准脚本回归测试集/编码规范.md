- 每个脚本/工具的测试文件顶部用 JSDoc 注释明确列出被断言的契约编号（如 (a)/(b)/(d)/(e)）与不变量清单，便于追踪回归来源。
- 需要仓库上下文的测试统一采用 mkdtemp + git init + 写入 fixture + spawnSync 执行脚本的模式，并在 afterAll/finally 中 rm -rf 清理。
- 对脚本文件发现规则的断言优先走 `--dry-run-list` 参数枚举结果，而非硬编码期望文件列表，使测试随仓库演进自适配。
- 对并行分发器的失败传播语义采用“合成 dispatcher”方式：在内存中写出共享相同 background-job+exit-file 聚合模式的微型脚本，避免改动真实 CHECKS 数组。
- 对纯函数（sharding.ts、mine-shard-weights.ts）的测试按 happy path / fallback semantics / invariants / balance quality 分组 describe，并显式断言确定性、N=1 边界与 RangeError 异常路径。