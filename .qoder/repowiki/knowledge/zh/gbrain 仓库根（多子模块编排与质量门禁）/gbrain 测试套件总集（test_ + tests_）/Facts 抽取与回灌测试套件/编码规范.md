- 每个需要持久层的测试文件遵循 `beforeAll` 创建单例 `PGLiteEngine` + `initSchema`，`afterAll` 断开连接，`beforeEach` 调用 `resetPgliteState(engine)` 清理数据。
- LLM 交互一律通过 `__setChatTransportForTests` 注入返回固定 `ChatResult` 的 stub，禁止真实网络请求。
- 异步任务类测试使用 `sleep(ms)` 小延迟配合 `getCounters()` 断言执行顺序与计数器，而非时间戳比较。
- 文件名以 `serial.test.ts` 结尾的用例被标记为串行执行，用于保护共享全局状态（如 MCP allowlist、上下文注入）。
- 测试注释以版本前缀（如 `v0.31 Phase 6`、`v0.42 Wave C2`）开头并列出所钉契约编号（D-EXTRACT-17/19/21），作为变更回归锚点。