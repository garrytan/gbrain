运行时无需配置 `DATABASE_URL`；每个需要 DB 的测试文件在 `beforeAll` 中 `new PGLiteEngine()` → `connect({})` → `initSchema()`，并在 `afterAll` 中 `disconnect()`；共享状态通过 `resetPgliteState(engine)`（来自 `test/helpers/reset-pglite.ts`）在 `beforeEach` 间 TRUNCATE 表，避免重复跑迁移。