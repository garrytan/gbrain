- 每个 describe 块以版本号或 issue 号前缀命名（如 `v0.42.20.0 — ...`、`#2084 — ...`），将断言与具体修复关联。
- 源码级回归断言统一采用 `readFileSync('src/...', 'utf8')` 后配合 `expect(src).toMatch(/.../)` 的正则匹配，而非导入模块执行逻辑。
- 对多 sink 注册类断言使用并列的 expect 链逐一校验各文件名中的 `name: 'xxx'` 片段，确保新增 sink 必须补全对应断言。