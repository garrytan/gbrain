- 使用 beforeAll 创建 PGLiteEngine 并 connect({}) + initSchema()，afterAll 断开连接，beforeEach 调用 resetPgliteState(engine) 清理表数据。
- 通过 registerBuiltinHandlers(fakeWorker, engine) 捕获已注册的 handler Map，再直接调用其函数签名 { id, name, data, signal }，绕过队列生命周期以专注被测逻辑。
- 对关键常量/CLI 行为采用源码文本断言（readFileSync(join(__dirname,'..','src',...).toString().toMatch(/CONST\s*=\s*value/))），而非仅断言运行结果，用于锁定回归点。
- 需要进程全局副作用（process.chdir、TTY 重设、mock.module）的测试文件显式命名为 *.serial.test.ts，以便被串行 runner 单独挑选执行。