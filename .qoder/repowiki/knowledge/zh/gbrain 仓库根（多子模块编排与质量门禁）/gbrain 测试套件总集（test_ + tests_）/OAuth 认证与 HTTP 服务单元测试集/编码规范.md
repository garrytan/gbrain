- 每个测试文件顶部以 JSDoc 注释说明所修复的具体 issue 编号或回归场景（如 #1166、v0.37.7.0、v0.41.3），并简述修复要点。
- 需要持久化状态的测试统一在 `beforeAll` 中构造 `PGLiteEngine` + `GBrainOAuthProvider`，在 `beforeEach` 中 `DELETE FROM oauth_tokens/oauth_codes/oauth_clients` 清理数据。
- HTTP 健康检查类测试通过本地工厂函数 `makeMockEngine` / `makeMockSql` 构造最小 mock，避免引入 Express 测试客户端。
- 环境变量相关测试使用共享 helper `./helpers/with-env.ts` 包裹，确保 GBRAIN_HTTP_CORS_ORIGIN 等变量隔离。
- 断言采用「happy path → timeout 路径 → db-error 路径」三段式结构，对 `result.ok` 分支分别校验 status/body/error_description。