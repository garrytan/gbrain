bun:test 运行器；通过 `../src/core/pglite-engine.ts` 连接内存版 PGLite 执行 schema 初始化与 SQL 断言；通过 `../src/core/minions/queue.ts` 操作 minion_jobs 表验证回填闸门；通过 `__setEmbedTransportForTests` 钩子替换 AI SDK 底层 transport 以零网络断言 gateway 路由。