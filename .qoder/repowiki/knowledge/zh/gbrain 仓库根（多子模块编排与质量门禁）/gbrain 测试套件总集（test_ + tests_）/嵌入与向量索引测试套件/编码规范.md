- 每个测试文件在 beforeAll 中创建单个 PGLiteEngine 实例并调用 initSchema，afterAll 中 disconnect，避免 beforeEach 重置导致迁移缓存失效。
- 需要修改全局 gateway 状态的测试在 afterAll 调用 resetGateway() 清理，防止 bun 分片间 OPENAI_API_KEY 等环境变量泄漏。
- 对可配置行为（冷却期、花费上限）同时覆盖“通过 opts 传入 override”和“通过 engine.setConfig 写入 config 表”两条路径。
- 错误分支统一断言抛出特定 Error 子类（如 EmbeddingCredentialError），并通过 diagnosis.reason / missingEnvVars 字段精确匹配诊断结果。
- 涉及 provider 覆盖的用例通过 installCaptureTransport 拦截 __setEmbedTransportForTests，断言 modelString 与 providerOptions.openaiCompatible.dimensions 而非发起真实 API 调用。