测试按被测模块一一对应组织：根级 `test/skillpack-*.test.ts` 与 `test/skill-*.test.ts` 分别映射 `src/core/skillpack/*` 与 `src/core/skill-catalog.ts` 等核心实现；`test/skillopt/` 下每个子文件对应一个 SkillOpt 子系统（rollout、audit、lock、benchmark、lr-schedule、held-out、reflect 等），其中 `skillopt/adversarial/` 专门用并发/崩溃/噪声 judge 等对抗场景验证锁与幂等。所有用例通过相对路径直接 import `../src/...` 源码，不依赖外部进程；需要 I/O 的用例以 `mkdtempSync` + `beforeAll/afterAll` 或 `withEnv` helper 构造临时工作区，确保 hermetic。并发类用例使用 `.serial.test.ts` 后缀配合 PGLiteEngine 与 `resetPgliteState` 来串行化数据库访问。