Bun 运行时 + PGLite（in-memory SQLite 引擎）用于单进程负载注入；Linux 下通过读取 `/proc/self/status` 的 RssAnon+RssShmem 精确采样峰值 RSS，非 Linux 回退到 `process.memoryUsage().rss`；Postgres 升级矩阵使用 `pgvector/pgvector:pg16` 镜像并通过 `psql` 执行 down-mutate SQL。