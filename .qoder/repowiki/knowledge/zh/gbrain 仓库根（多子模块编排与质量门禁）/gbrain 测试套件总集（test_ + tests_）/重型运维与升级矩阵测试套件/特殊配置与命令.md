- 本地全量运行：`bun run test:heavy`（由 `scripts/run-heavy.sh` 驱动）
- 单个脚本：直接执行 `tests/heavy/<script>.sh`
- RSS 基线刷新（仅限 Linux）：`tests/heavy/measure_rss.sh --refresh-baseline`，推荐在容器内执行以避免 VmRSS 污染基线
- 升级矩阵需外部 Postgres：先启动 `docker run -d --name gbrain-test-pg -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=gbrain_test -p 5434:5432 pgvector/pgvector:pg16`，再 `export DATABASE_URL=postgresql://postgres:postgres@localhost:5434/gbrain_test` 后运行 `pg_upgrade_matrix.sh`