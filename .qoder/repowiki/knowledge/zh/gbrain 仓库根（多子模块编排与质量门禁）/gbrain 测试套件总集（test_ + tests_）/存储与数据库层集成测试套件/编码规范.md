- 每个 describe 块以 beforeEach(truncateAll) 或自定义清理函数重置 PGLite 表，确保测试间无状态泄漏。
- 需要真实磁盘/YAML 的测试使用 mkdtempSync 在 tmpdir 下创建隔离目录，并在 finally 中 rmSync 清理。
- 涉及进程全局副作用（process.chdir、process.env、gateway 单例）的测试统一重命名为 *.serial.test.ts 并加注释说明串行原因。
- 对连接池 GUC 泄漏等安全属性的测试采用源码级正则断言（readFileSync + stripComments + regex），而非运行时注入。
- 对外部依赖（StorageBackend、BrainEngine.executeRaw）通过内联工厂函数返回 stub，记录调用序列以便断言幂等与崩溃恢复语义。