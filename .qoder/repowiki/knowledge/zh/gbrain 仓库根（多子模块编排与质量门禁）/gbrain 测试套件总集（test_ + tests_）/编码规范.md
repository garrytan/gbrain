- 所有用例文件以 .test.ts 或 .serial.test.ts 后缀命名，串行用例显式标注 serial 后缀
- 每个子系统目录自带 fixtures/ 子目录存放该域专用的 JSON/SQL/Markdown 夹具，避免跨域污染
- 需要外部依赖（Postgres/PGLite/HTTP 服务）的用例通过 test/helpers 中的 withEnv、PTY runner、spawn 辅助函数启动隔离环境
- 涉及版本迁移的用例按 v0_X_Y 前缀组织，对应 src/migrations 中的具体迁移文件