# v0.57.0.11

- `gbrain backup create --json` / `restore --json` failures always carry a
  `message`: engine errors without `.message` (e.g. PGLite `ErrnoError`)
  fall back to `String(error)` instead of emitting a bare `backup_failed`.
  No action needed.
