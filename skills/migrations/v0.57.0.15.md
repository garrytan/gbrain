# v0.57.0.15

- Provider `model_not_found` 404s on chat/expansion now name the config key that selected the model and the `gbrain config set` fix (#5304): `Selected via models.tier.reasoning. Fix: gbrain config set models.tier.reasoning <provider>:<model>`. Tier-default selections point at `models.tier.<tier>`; explicit per-call models get `gbrain config get models.tier.<tier>` / `gbrain doctor` as the discovery hint. No action needed.
