# v0.57.0.14: ref'd guard timer in pool teardown

No upgrade action is required. Behavior is unchanged on platforms where
unref'd timers are serviced; on Windows under `bun test` (and any other
environment that starves unref'd timers) the pool-teardown bound now fires
as designed instead of never resolving.
