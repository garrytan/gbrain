# v0.57.0.34 — quote repairs begin on a word boundary

`normalizeForGrounding` collapsed a whitespace run into one space whose
offset map pointed at the previous word's last character. The quote
verify repair then sliced the original starting inside that word —
republished quotes like "g every approved frame".

The collapsed space now maps to the run's first whitespace character, so
every consumer slicing via the map lands on real whitespace. A trimmed
replacement begins on a word boundary.

No action required.
