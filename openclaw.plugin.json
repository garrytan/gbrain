{
  "name": "mbrain",
  "version": "0.11.0",
  "description": "Postgres + pgvector personal knowledge brain with explicit SQLite/offline compatibility",
  "family": "bundle-plugin",
  "configSchema": {
    "database_url": {
      "type": "string",
      "required": false,
      "description": "Optional PostgreSQL connection URL; otherwise use the configured MBrain Postgres profile or environment",
      "uiHints": { "sensitive": true }
    },
    "openai_api_key": {
      "type": "string",
      "required": false,
      "description": "Optional OpenAI API key for hosted features (uses OPENAI_API_KEY env var if not set)",
      "uiHints": { "sensitive": true }
    }
  },
  "mcpServers": {
    "mbrain": {
      "command": "bun",
      "args": ["run", "src/cli.ts", "serve"]
    }
  },
  "skills": [
    "skills/ingest",
    "skills/query",
    "skills/maintain",
    "skills/enrich",
    "skills/briefing",
    "skills/migrate",
    "skills/setup"
  ],
  "openclaw": {
    "compat": {
      "pluginApi": ">=2026.4.0"
    }
  }
}
