---
title: "JS Function for Bumble Messaging"
type: guide
created: 2023-01-31
updated: 2023-01-31
source: chatgpt-export
conversation_id: 1ff7d050-c949-4ad0-930a-ad985f70b844
message_count: 8
tags: [chatgpt, import, code]
---
# JS Function for Bumble Messaging

> Conversation ID: 1ff7d050-c949-4ad0-930a-ad985f70b844
> Created: 2023-01-31T05:37:49Z
> Updated: 2023-01-31T05:37:49Z
> Messages: 8

---

## User


function messageAllBumbleContacts() {

    // Define the XPath expression to find all message blocks with the class "contact-avatar contact-avatar--color-expiration-status-average"
    const xpathExpression = "//div[contains(@class, 'contact-avatar contact-avatar--color-expiration-status-average')]/ancestor::div[contains(@class, 'contact')]";

    // Evaluate the XPath expression to get a list of all matching elements
    const result = document.evaluate(xpathExpression, document, null, XPathResult.ORDERED_NODE_SNAPSHOT_TYPE, null);

    console.log(`Found ${result.snapshotLength} new thread(s) with no replies`);
    //set a delay for 1 second


    // Iterate through the list of matching elements
    for (let i = 0; i < result.snapshotLength; i++) {
        const contact = result.snapshotItem(i);
        const name = contact.getAttribute("data-qa-name");
        console.log(`New thread with no replies: ${name}`);

        // Click on the element
        contact.click();

        //delay for 1 second


        // check if the text input exist 
        const textArea = document.querySelector(".textarea__input");
        if (textArea) {
            console.log(message || `textarea found, inserting text: "Hey there ${name}!`);
            /*   textArea.innerHTML = `Hey there ${name}!`; */
        } else {
            console.log("textarea not found");
        }
    }


}

---

## Assistant

This JavaScript function is for automating the process of sending messages to all contacts on a dating app like Bumble. It uses XPath to find all the message blocks with the class "contact-avatar contact-avatar--color-expiration-status-average". It evaluates the XPath expression and logs the number of matching elements it finds. Then, it iterates through the list of matching elements, logs the name of each new thread, clicks on the element, and checks if the text input exists. If the text input exists, it logs a message saying it was found and inserts the text "Hey there [name]!" into the text area. If the text input does not exist, it logs a message saying it was not found.

---

## User

make it pause for 5 - 10 seconds between each message click

---

## Assistant

You can add a delay between each message click by using `setTimeout` function in JavaScript. Here's an updated version of the code:

```
function messageAllBumbleContacts() {
  const delay = Math.floor(Math.random() * (10000 - 5000 + 1) + 5000);

  // Define the XPath expression to find all message blocks with the class "contact-avatar contact-avatar--color-expiration-status-average"
  const xpathExpression = "//div[contains(@class, 'contact-avatar contact-avatar--color-expiration-status-average')]/ancestor::div[contains(@class, 'contact')]";

  // Evaluate the XPath expression to get a list of all matching elements
  const result = document.evaluate(xpathExpression, document, null, XPathResult.ORDERED_NODE_SNAPSHOT_TYPE, null);

  console.log(`Found ${result.snapshotLength} new thread(s) with no replies`);

  // Iterate through the list of matching elements
  for (let i = 0; i < result.snapshotLength; i++) {
    const contact = result.snapshotItem(i);
    const name = contact.getAttribute("data-qa-name");
    console.log(`New thread with no replies: ${name}`);

    setTimeout(() => {
      // Click on the element
      contact.click();

      // check if the text input exist 
      const textArea = document.querySelector(".textarea__input");
      if (textArea) {
        console.log(`textarea found, inserting text: "Hey there ${name}!`);
        /*   textArea.innerHTML = `Hey there ${name}!`; */
      } else {
        console.log("textarea not found");
      }
    }, delay);
  }
}
```

This code adds a random delay of 5 to 10 seconds between each message click by using `setTimeout` function. The delay is generated by `Math.floor(Math.random() * (10000 - 5000 + 1) + 5000)` which generates a random number between 5000 and 10000 (both inclusive).

---

## User

add an alert when done

---

## Assistant

You can add an alert when all messages have been sent by adding an `if` statement after the for loop. Here's an updated version of the code:

```
function messageAllBumbleContacts() {
  const delay = Math.floor(Math.random() * (10000 - 5000 + 1) + 5000);

  // Define the XPath expression to find all message blocks with the class "contact-avatar contact-avatar--color-expiration-status-average"
  const xpathExpression = "//div[contains(@class, 'contact-avatar contact-avatar--color-expiration-status-average')]/ancestor::div[contains(@class, 'contact')]";

  // Evaluate the XPath expression to get a list of all matching elements
  const result = document.evaluate(xpathExpression, document, null, XPathResult.ORDERED_NODE_SNAPSHOT_TYPE, null);

  console.log(`Found ${result.snapshotLength} new thread(s) with no replies`);

  // Iterate through the list of matching elements
  for (let i = 0; i < result.snapshotLength; i++) {
    const contact = result.snapshotItem(i);
    const name = contact.getAttribute("data-qa-name");
    console.log(`New thread with no replies: ${name}`);

    setTimeout(() => {
      // Click on the element
      contact.click();

      // check if the text input exist 
      const textArea = document.querySelector(".textarea__input");
      if (textArea) {
        console.log(`textarea found, inserting text: "Hey there ${name}!`);
        /*   textArea.innerHTML = `Hey there ${name}!`; */
      } else {
        console.log("textarea not found");
      }

      if (i === result.snapshotLength - 1) {
        alert("All messages have been sent");
      }
    }, delay);
  }
}
```

This code adds an alert that says "All messages have been sent" when all messages have been sent. The alert is triggered by checking if `i` is equal to `result.snapshotLength - 1` which means that the for loop has completed its iteration.

---

## User

can we parst the json coming back from the page response instead of scraping the html?

---

## Assistant

Yes, you can parse the JSON response from the page instead of scraping the HTML. The exact implementation would depend on how the JSON data is returned from the page, but here is an example using `fetch` to make a GET request to an API endpoint and parse the JSON response:

```
fetch("https://api.example.com/data")
  .then(response => response.json())
  .then(data => {
    console.log(data);
    // Use the data as needed
  })
  .catch(error => {
    console.error(error);
  });
```

In this example, `fetch` is used to make a GET request to the URL `https://api.example.com/data`. The response from the API is then passed to `response.json()` to parse the JSON data. The parsed data is then logged to the console and can be used as needed in your code. If there is an error with the request, the error is logged to the console using `console.error`.


## See Also
- [[chatgpt/chatgpt_chrome-extension-function-location]]
- [[chatgpt/chatgpt_fix-email-validation-function]]
- [[chatgpt/chatgpt_fix-javascript-function]]
- [[chatgpt/chatgpt_html-bumble-messages-unknown]]
- [[chatgpt/chatgpt_improve-js-function]]
